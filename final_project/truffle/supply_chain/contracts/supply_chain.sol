pragma solidity ^0.5.0;

// API and some comments from https://ecen5033.org/proj2.pdf

contract SupplyChain {
    
    function always_passes() public pure returns (bool) {
        return true;
    }
}
